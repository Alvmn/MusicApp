<%= form_tag(instrument_song_comment_path(params[:instrument_id], params[:song_id], @comment.id), method: "put") do %>
	<%= text_area_tag(:content, @comment.content, placeholder:"What do you think?...", size: "24x12") %>
	<%= submit_tag('Submit', method: :put) %>
<% end %>