<div class='row'>
<div class="small-10 small-offset-2 columns">
<!-- <div class="try"> -->
	<div class="row">	
		<div class="small-9 small-offset-3 columns">
		<p><img src="/images/logo3.png" class="logo"/></p>
		</div>
	</div>	

		<div class="small-9 small-offset-5 colunms">
			<% if flash[:alert] %>
			    <div class="notice"><%= flash[:alert] %></div>
			  <% end %>
			
			
			
		</div>
	<!-- </div> -->
<%= form_tag({controller: "instruments", action: "found_songs"}, {method: :post}) do %>
	<%= text_area_tag(:title,"", placeholder: "Buscar canción por título", size: "24x6") %>
	<%= submit_tag("Submit") %>
<% end %>
<%= form_tag({controller: "instruments", action: "found_songs"}, {method: :post}) do %>
	<%= text_area_tag(:songwriter,"", placeholder: "Buscar canción por autor", size: "24x6") %>
	<%= submit_tag("Submit") %>
<% end %>

	<div class="row boxi">
			<%= render @instruments %>			
	</div>	
	<div class="row">
		<div class="small-2 small-offset-1 columns">
		<% if current_user && current_user.has_role?(:admin) %>
			<%= button_to('Add instrument', new_instrument_path, method: :get) %>
		<% end %>
		
		</div>	
	</div>			
</div>	
</div>
</div>

<!-- <embed src="/flash/clockdig.swf"  class="clock" width="160" height="40" play="true"  loop="true" quality="high" > 
</embed> 
 -->